# RenderLab Project Structure
**Date:** 29.11.2025  
**Total Files:** 207 TypeScript/TSX files

---

## 📁 App Router (`app/`)

### Core Pages
```
app/
├── layout.tsx                    # Root layout — wraps all pages, includes providers & navbar
├── error.tsx                     # Error boundary — catches React errors, shows fallback UI
├── global-error.tsx              # Global error handler — catches root-level errors
├── middleware.ts                 # Auth middleware — protects routes, redirects unauthenticated users
│
├── workspace/                    # 🎨 Main Workspace
│   ├── page.tsx                  # Server component — auth check, loads history images from Supabase
│   └── WorkspaceClientV2.tsx     # Client component — upload panel, prompt builder, pills, generate button, realtime updates
│
├── batch/                        # ⚡ Batch Studio
│   └── page.tsx                  # Multi-image generation — select collection, upload reference, stream results with progress
│
├── inpaint/                      # 🖌️ Inpaint Editor
│   └── page.tsx                  # Mask-based editing — canvas drawing, brush tools, mask extraction, generation
│
├── history/                      # 📜 Generation History
│   ├── page.tsx                  # Paginated history — loads images with infinite scroll, delete/download actions
│   └── HistoryGrid.tsx           # Grid component — renders image cards with hover actions, modal preview
│
├── collections/                  # 📦 Collections
│   └── page.tsx                  # Redirect — forwards to /custom?tab=collections
│
├── custom/                       # ⚙️ Custom Templates & Collections
│   └── page.tsx                  # Tabs UI — switch between Templates and Collections panels, CRUD operations
│
├── templates/                    # 📋 Template Gallery
│   └── page.tsx                  # Redirect — forwards to /custom?tab=templates
│
├── prompts/                      # 💡 Community Prompts
│   ├── page.tsx                  # Prompts library — filterable list, like/bookmark, copy to workspace
│   └── submit/
│       └── page.tsx              # Submit form — create new prompt with image, category, tags
│
├── profile/                      # 👤 User Profile
│   └── page.tsx                  # Profile settings — display name, avatar, preferences
│
├── account/                      # 💳 Account Settings
│   └── page.tsx                  # Account management — subscription, billing, usage stats
│
└── landing-preview/              # 🔍 Landing Preview
    └── page.tsx                  # Preview mode — test landing page components in isolation
```

### Marketing Pages (`app/(marketing)/`)
```
app/(marketing)/
├── layout.tsx                    # Marketing layout — different navbar, no auth required
├── page.tsx                      # Landing page — Hero parallax, Companies, Features, Templates, Testimonials, CTA
├── pricing/
│   ├── page.tsx                  # Pricing page — tier comparison, feature lists
│   └── pricing-table.tsx         # Pricing table component — interactive tier cards
├── blog/
│   └── page.tsx                  # Blog listing — MDX posts, categories
└── contact/
    └── page.tsx                  # Contact form — email submission, support info
```

### Auth Pages (`app/(auth)/`)
```
app/(auth)/
├── layout.tsx                    # Auth layout — centered form, minimal UI
├── login/
│   └── page.tsx                  # Login form — email/password, OAuth, remember me
├── signup/
│   └── page.tsx                  # Signup form — registration with email verification
└── forgot-password/
    └── page.tsx                  # Password reset — email input, sends reset link
```

---

## 🔌 API Routes (`app/api/`)

### Generation
```
app/api/
├── generate/
│   ├── route.ts                  # Single generation — calls Replicate API, saves to DB, returns image URL
│   ├── edit/
│   │   └── route.ts              # Edit existing — modify image with new prompt
│   ├── collection/
│   │   └── route.ts              # Collection generation — batch process entire collection
│   └── collection-preview/
│       └── route.ts              # Batch preview — streaming responses with p-limit concurrency, progress events
│
├── inpaint/
│   └── nano-banana/
│       └── route.ts              # Inpaint API — accepts mask + image, calls nano-banana model
│
└── generate-thumbnail/
    └── route.ts                  # Thumbnail generation — creates 200x200 WebP from full image
```

### Data Management
```
app/api/
├── images/
│   ├── route.ts                  # List images — paginated, filtered by user_id
│   └── [id]/
│       ├── route.ts              # Single image — GET/DELETE operations
│       └── hide/
│           └── route.ts          # Hide image — soft delete from history view
│
├── prompts/
│   ├── route.ts                  # List prompts — public library with filters
│   ├── create/
│   │   └── route.ts              # Create prompt — insert with image upload
│   ├── [id]/
│   │   ├── route.ts              # Single prompt — GET/UPDATE/DELETE
│   │   └── like/
│   │       └── route.ts          # Like prompt — toggle like, update count
│   └── user/
│       └── [userId]/
│           └── route.ts          # User prompts — list prompts by user
│
├── credits/
│   └── route.ts                  # User credits — check balance, deduct on generation
│
├── subscribe/
│   └── route.ts                  # Email subscription — add to waitlist, check duplicates
│
└── upload/
    └── route.ts                  # File upload — stores to Supabase Storage, returns URL
```

### Admin
```
app/api/admin/
├── queue-status/
│   └── route.ts                  # Queue status — check generation queue length, processing time
└── prompts/
    └── moderate/
        └── route.ts              # Moderate prompts — approve/reject submitted prompts
```

---

## 🧩 Components (`components/`)

### UI Components (`components/ui/`)
```
components/ui/
├── RenderLabButton.tsx           # 🔶 Primary button — gradient/outline/filled variants, loading state
├── RenderLabInput.tsx            # Input field — styled with focus ring, error state
├── RenderLabSelect.tsx           # Select dropdown — custom styling, icons
├── RenderLabModal.tsx            # Modal dialog — backdrop blur, animations
├── RenderLabToast.tsx            # Toast notifications — success/error/info variants
├── RenderLabToaster.tsx          # Toaster provider — global toast container
├── PremiumButton.tsx             # Premium CTA button — animated glow effect
├── SkeletonGrid.tsx              # Loading skeleton — pulsing placeholder grid
├── Tooltip.tsx                   # Tooltip — hover info, positioning
│
├── button.tsx                    # Base button (shadcn) — variant system
├── card.tsx                      # Card component — container with border/shadow
├── dialog.tsx                    # Dialog — modal with trigger
├── dropdown-menu.tsx             # Dropdown — menu with items, separators
├── input.tsx                     # Input — base input element
├── label.tsx                     # Label — form label with for attribute
├── select.tsx                    # Select — native select wrapper
├── tabs.tsx                      # Tabs — tab list with panels
├── form.tsx                      # Form — react-hook-form integration
├── sonner.tsx                    # Sonner toast — toast library config
│
├── hero-parallax.tsx             # 🎬 Hero with parallax — scroll-based motion, product cards
├── spotlight.tsx                 # Spotlight effect — mouse-following gradient
├── background-beams.tsx          # Background beams — animated SVG lines
├── avatar-circles.tsx            # Avatar stack — overlapping user avatars
├── animated-tooltip.tsx          # Animated tooltip — spring animation on hover
├── container-scroll-animation.tsx # Scroll animation — reveal on scroll
├── parallax-scroll.tsx           # Parallax scroll — depth layers
├── sticky-scroll-reveal.tsx      # Sticky reveal — content pins while scrolling
└── infinite-moving-cards.tsx     # Moving cards — infinite horizontal scroll
```

### Workspace Components (`components/workspace/`)
```
components/workspace/
├── WorkspaceLayoutV2.tsx         # 📐 Main layout — 2-column grid, history panel, greeting header
├── ImageUploadPanel.tsx          # Upload zone — drag/drop, file input, preview, clear button
├── ImagePreviewModal.tsx         # Fullscreen preview — image zoom, navigation, download
├── PromptBuilderPanel.tsx        # Prompt builder — textarea, pills, template loading
├── PromptBuilderPanelNew.tsx     # New prompt builder — updated UI with semantic pills
├── CollectionPanel.tsx           # Collection selector — CRUD for collections, template management
├── PreviewPanel.tsx              # Image preview — displays selected/generated image
├── PreviewStrip.tsx              # Thumbnail strip — horizontal scrolling previews
├── WorkspacePreviewStrip.tsx     # Workspace preview — bottom strip variant
├── ActionsPanel.tsx              # Action buttons — generate, clear, download
├── ContextIndicator.tsx          # Context display — shows loaded template/collection
├── PanelWrapper.tsx              # Panel container — consistent styling wrapper
├── PromptTemplates.tsx           # Template list — saved templates with edit/delete
├── InpaintOverlay.tsx            # Inpaint overlay — mask visualization on image
├── uploadReferenceImageOnce.ts   # Upload utility — prevents duplicate uploads
│
└── prompt-builder/
    ├── ModelSelector.tsx         # AI model selector — dropdown with model descriptions
    ├── ActionButtons.tsx         # Generate/Clear — primary action buttons
    ├── PromptPreview.tsx         # Prompt preview — shows assembled prompt
    ├── BookmarkSelector.tsx      # Saved prompts — quick load from bookmarks
    └── TemplateBuilder.tsx       # Template editor — create/edit template form
```

### Inpaint Components (`components/inpaint/`)
```
components/inpaint/
├── CanvasArea.tsx                # Drawing canvas — fabric.js canvas, brush strokes, undo/redo
├── TopControls.tsx               # Top toolbar — zoom, fit, reset buttons
├── BottomToolbar.tsx             # Bottom toolbar — generate, clear mask, download
├── ToolIconsBar.tsx              # Tool icons — brush, eraser, size selector
├── BrushControls.tsx             # Brush settings — size slider, color picker
└── ResultView.tsx                # Result display — shows generated result with compare
```

### Navbar (`components/navbar/`)
```
components/navbar/
├── index.tsx                     # Export — re-exports all navbar components
├── AppNavbar.tsx                 # App navbar — authenticated user nav with dropdown menu
├── NavbarWrapper.tsx             # Wrapper — handles navbar visibility/scroll behavior
├── desktop-navbar.tsx            # Desktop nav — full horizontal navigation
├── mobile-navbar.tsx             # Mobile nav — hamburger menu, slide-out drawer
├── UserMenu.tsx                  # User dropdown — profile, settings, logout
├── UseCredits.tsx                # Credits display — shows remaining credits with badge
└── navbar-item.tsx               # Nav item — single navigation link with active state
```

### Landing Components
```
components/
├── hero-renderlab.tsx            # Hero section — headline, subtext, CTA buttons
├── hero.tsx                      # Alternative hero — different layout variant
├── cta.tsx                       # CTA section — email form, early access signup
├── cta-renderlab.tsx             # RenderLab CTA — styled CTA variant
├── grid-features.tsx             # Features grid — 8 feature cards with icons, descriptions
├── features.tsx                  # Features list — vertical feature showcase
├── testimonials.tsx              # Testimonials — customer quotes carousel
├── testimonials-renderlab.tsx    # RenderLab testimonials — styled variant
├── featured-testimonials.tsx     # Featured testimonials — highlighted reviews
├── testimonial-column-container.tsx # Column container — testimonial layout wrapper
├── pricing.tsx                   # Pricing — tier cards with features
├── pricing-renderlab.tsx         # RenderLab pricing — custom pricing component
├── companies.tsx                 # Trusted by logos — company logo strip
├── how-it-works.tsx              # How it works — step-by-step process
├── before-after-showcase.tsx     # Before/after — image comparison slider
├── globe.tsx                     # 3D globe — interactive globe animation
├── grid-lines.tsx                # Grid pattern — subtle background grid
├── background.tsx                # Background — gradient/pattern backgrounds
├── horizontal-gradient.tsx       # Gradient — horizontal gradient overlay
└── footer.tsx                    # Footer — links, social, copyright
```

### Prompts Components (`components/prompts/`)
```
components/prompts/
├── PromptCard.tsx                # Prompt card — displays prompt with image, likes, actions
├── FilterBar.tsx                 # Filter controls — category, badge, search filters
├── ImageUpload.tsx               # Image upload — for prompt submission
└── SubmitPromptForm.tsx          # Submit form — full form for new prompt
```

### Batch Components (`components/batch/`)
```
components/batch/
└── ErrorCard.tsx                 # Error display — shows failed generation with retry
```

### Common Components (`components/common/`)
```
components/common/
└── ImagePreviewModal.tsx         # Shared modal — reusable image preview across pages
```

### Layout Components
```
components/layout/
├── RenderLabLayout.tsx           # Main layout — wraps app with theme, providers
├── LayoutContent.tsx             # Content wrapper — max-width container
└── MainNavbar.tsx                # Main navbar — switches between marketing/app navbars
```

### Providers (`components/providers/`)
```
components/providers/
├── SupabaseAuthProvider.tsx      # Auth provider — Supabase session management, user context
└── supabase-auth-provider.tsx    # Alt provider — legacy auth provider
```

### Other Components
```
components/
├── Logo.tsx                      # RenderLab logo — SVG logo with text
├── container.tsx                 # Container — max-width wrapper with padding
├── heading.tsx                   # Heading — styled h1-h6 components
├── subheading.tsx                # Subheading — secondary text styling
├── badge.tsx                     # Badge — small label/tag component
├── button.tsx                    # Button — base button component
├── blur-image.tsx                # Blur image — progressive image loading with blur
├── contact.tsx                   # Contact form — name, email, message fields
├── login.tsx                     # Login form — email/password inputs
├── signup.tsx                    # Signup form — registration inputs
├── password.tsx                  # Password input — show/hide toggle
├── mode-toggle.tsx               # Dark/light toggle — theme switcher
├── switch.tsx                    # Switch — toggle switch component
├── blog-card.tsx                 # Blog card — post preview with image
├── blog-layout.tsx               # Blog layout — MDX content wrapper
├── BuildPanel.tsx                # Build panel — dev tools panel
├── in-view-div.tsx               # In-view trigger — intersection observer wrapper
└── skeletons/
    └── second.tsx                # Skeleton variants — different loading states
```

---

## 📚 Library (`lib/`)

### Supabase Clients
```
lib/
├── supabaseClient.ts             # Generic client — basic Supabase client setup
├── supabaseServer.ts             # Server client — RSC-compatible client with cookies
└── supabaseBrowser.ts            # Browser client — client-side Supabase instance
```

### Core Logic
```
lib/
├── generateSingle.ts             # Single generation logic — Replicate API call, model mapping, URL extraction
├── promptAssembler.ts            # Prompt assembly — combines base prompt with modifiers
├── utils.ts                      # General utilities — cn(), formatDate, etc.
├── toast-config.ts               # Toast configuration — default toast styles
├── constants.tsx                 # App constants — model names, limits, defaults
├── blog.ts                       # Blog utilities — MDX parsing, frontmatter
└── z-layer-guide.ts              # Z-index guide — z-index reference values
```

### Hooks (`lib/hooks/`)
```
lib/hooks/
├── useTemplates.ts               # Templates hook — CRUD operations for templates
└── useCollections.ts             # Collections hook — CRUD operations for collections
```

### Context (`lib/context/`)
```
lib/context/
└── WorkspaceContext.tsx          # Workspace state — active template/collection, load/clear functions
```

### Types (`lib/types/`)
```
lib/types/
├── database.ts                   # DB types — Supabase table types, ActiveItem, Template, Collection
└── prompts.ts                    # Prompt types — Prompt, PromptCategory, PromptBadge
```

### Constants (`lib/constants/`)
```
lib/constants/
└── inpaint.ts                    # Inpaint constants — brush sizes, colors, defaults
```

### Utilities (`lib/utils/`)
```
lib/utils/
├── uploadToStorage.ts            # Storage upload — upload file to Supabase Storage bucket
├── requestQueue.ts               # Request queue — rate limiting for API calls
│
├── gemini/
│   ├── client.ts                 # Gemini client — Google AI client setup
│   └── urlToBase64.ts            # URL to base64 — converts image URL to base64 string
│
└── inpaint/
    ├── maskExtractor.ts          # Mask extraction — extracts mask from canvas as PNG
    ├── maskConverter.ts          # Mask conversion — converts mask format for API
    ├── maskVisualizer.ts         # Mask visualization — renders mask overlay
    └── promptBuilder.ts          # Inpaint prompt — builds prompt for inpaint model
```

### Database (`lib/db/`)
```
lib/db/
└── prompts.ts                    # Prompts DB operations — getPrompts, createPrompt, likePrompt
```

---

## 🗄️ Other Directories

### Core (`core/`)
```
core/
└── thumbnail/
    └── createClientThumbnail.ts  # Client-side thumbnail — resizes image to 200x200 WebP
```

### Scripts (`scripts/`)
```
scripts/
├── cleanup-old-images.ts         # Image cleanup — deletes images older than X days
├── analyze-storage.ts            # Storage analysis — reports storage usage by user
└── backfill-thumbnails.ts        # Thumbnail backfill — generates missing thumbnails
```

### Supabase Functions (`supabase/functions/`)
```
supabase/functions/
└── cleanup-history/
    └── index.ts                  # History cleanup edge function — scheduled cleanup of old images
```

### Layouts (`layouts/`)
```
layouts/
└── auth-layout.tsx               # Auth layout — centered card layout for auth pages
```

### Constants (`constants/`)
```
constants/
└── tier.tsx                      # Pricing tiers — Free, Pro, Enterprise definitions
```

---

## 🎨 Styles

### CSS Files
```
app/
├── globals.css                   # Global styles — base styles, Tailwind imports
├── renderlab-theme.css           # 🔶 RenderLab theme — CSS variables for colors, spacing
├── design-tokens.css             # Design tokens — typography, shadows, borders
├── components-primitives.css     # Component primitives — reusable class patterns
└── prism.css                     # Code highlighting — syntax highlighting for code blocks

styles/                           # Additional styles folder
```

### Key CSS Variables (`renderlab-theme.css`)
```css
--rl-accent: #ff6b35;             /* Orange accent */
--rl-bg: #0a0a0a;                 /* Background */
--rl-text: #ffffff;               /* Text */
--rl-text-secondary: #a3a3a3;     /* Secondary text */
--rl-border: rgba(255,255,255,0.1);
```

---

## 📦 Config Files

```
/
├── package.json                  # Dependencies & scripts
├── next.config.mjs               # Next.js configuration
├── tailwind.config.ts            # Tailwind CSS config
├── tsconfig.json                 # TypeScript config
├── jsconfig.json                 # JS paths config
├── eslint.config.mjs             # ESLint rules
├── postcss.config.mjs            # PostCSS plugins
├── components.json               # shadcn/ui config
├── middleware.ts                 # Auth middleware
├── mdx-components.tsx            # MDX custom components
└── next-env.d.ts                 # Next.js type declarations
```

---

## 🔑 Key Routes Summary

| Route | Purpose |
|-------|---------|
| `/` | Landing page — Hero, Features, Testimonials, CTA |
| `/workspace` | Main AI workspace — Upload, Prompt, Generate |
| `/batch` | Batch generation — Process entire collection |
| `/inpaint` | Mask-based editing — Draw mask, regenerate area |
| `/history` | Generation history — View all generated images |
| `/collections` | Collections manager — Redirects to /custom |
| `/custom` | Custom templates & collections — CRUD interface |
| `/templates` | Template gallery — Redirects to /custom |
| `/prompts` | Community prompts — Browse & share prompts |
| `/pricing` | Pricing page — Subscription tiers |
| `/login` | Login — Email/password auth |
| `/signup` | Sign up — Create account |
| `/profile` | User profile — Settings & preferences |
| `/account` | Account settings — Billing & subscription |

---

## 🔶 RenderLab Design System

### Colors
- **Accent:** `#ff6b35` (Orange)
- **Background:** `#0a0a0a` (Dark)
- **Text:** `#ffffff` / `#a3a3a3`

### Components
- `RenderLabButton` — Primary button with gradient/outline variants
- `RenderLabInput` — Styled input field
- `RenderLabModal` — Modal dialog
- `RenderLabSelect` — Dropdown select
- `RenderLabToast` — Toast notifications

### Buttons
```tsx
<RenderLabButton variant="gradient">Primary</RenderLabButton>
<RenderLabButton variant="outline">Secondary</RenderLabButton>
```

---

*Generated: 29.11.2025*
